name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "20"
      - name: Verify release
        run: npm test
      - name: Scan public content
        run: npm run scan:public

  release:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: Build archive and checksum
        shell: bash
        run: |
          archive="GrillMeJewel-${GITHUB_REF_NAME}.zip"
          git archive --format=zip --prefix="GrillMeJewel-${GITHUB_REF_NAME}/" -o "$archive" "$GITHUB_REF_NAME"
          sha256sum "$archive" > "$archive.sha256"
      - name: Publish GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          generate_release_notes: true
          files: |
            GrillMeJewel-${{ github.ref_name }}.zip
            GrillMeJewel-${{ github.ref_name }}.zip.sha256
